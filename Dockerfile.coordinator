FROM openjdk:17-jdk-slim

WORKDIR /app

# Copy the entire project
COPY . .

# Build the coordinator application
RUN ./gradlew :coordinator:build -x test

# Expose the coordinator port
EXPOSE 8080

# Run the coordinator application
CMD ["java", "-jar", "coordinator/build/libs/coordinator-0.0.1-SNAPSHOT.jar"] 